import type {GatsbyConfig} from "gatsby"

const config: GatsbyConfig = {
  siteMetadata: {
    title: `Lab interfejsy`,
    siteUrl: `https://www.kssk.pwr.edu.pl`,
  },
  plugins: [`gatsby-theme-material-ui`],
}

export default config
